name: Sonde proj_id BM
on:
  workflow_dispatch:
jobs:
  sonde-projid:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-radar.txt
      - name: Inspecter un record BM procnotices
        run: python sonde_projid.py
